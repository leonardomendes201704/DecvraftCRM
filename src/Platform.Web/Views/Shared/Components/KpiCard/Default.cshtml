@model Platform.Web.ViewModels.Components.KpiCardViewModel

<section class="kpi-card">
    <span class="kpi-label">@Model.Label</span>
    <strong class="kpi-value">@Model.Value</strong>
    <span class="kpi-context">@Model.Context</span>
</section>
